{
  "name": "nowa-dev-agent-relay",
  "version": "1.0.0",
  "description": "Agent relay protocol — lets two Claude agents communicate directly via a hosted API",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
